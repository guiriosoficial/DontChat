      </ChatContainer>
      <ChatContainer>
        {errorMessage && <ErrorMessage>{errorMessage}</ErrorMessage>}
import { ChatContainer, ErrorMessage } from './styles'
      <ChatContainer>
        {errorMessage && <ErrorMessage>{errorMessage}</ErrorMessage>}
      </ChatContainer>
